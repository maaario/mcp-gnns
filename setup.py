from setuptools import find_packages, setup

setup(name='clique_finding_models',
      description='clique finding models and infrastructure',
      version='0.0.1-dev',
      install_requires=[],
      packages=find_packages(),
      zip_safe=False)